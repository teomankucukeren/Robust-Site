# Robust Simple — çalışma notları

- Aksi belirtilmedikçe, kullanıcının istediği tüm değişiklikler **tablet** sürümü içindir.
  Tablet = `@media (min-width: 761px) and (max-width: 960px)` (önizleme: 834×1194).
  Telefon <760px `MobileWorks` bileşenini, tablet ise masaüstü funnel'ını kullanır.
